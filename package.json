{
  "name": "my-turborepo",
  "private": true,
  "scripts": {
    "start": "turbo dev",
    "build": "turbo build",
    "seed": "turbo seed --filter=@monorepo/backend",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.3.2",
    "turbo": "^2.0.6"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
